#include "Card.h"

static class CardDictionary {
	void GetCardImage(Card &card) {
		if (card.getName() == "Ace") {
			switch (card.getSuit()) {
			case "spades":
				return IMG_LoadTexture
			}
		}
	}
};